.message{message_id: "#{message.id}"}
  %h3.message__name= message.user.name
  %p.message__date= message.created_at.to_s
  %p.message__content= message.content
  - if message.image?
    = image_tag message.image, class: "message__image"